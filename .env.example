# Copy to .env only if using paid OpenAI coaching (--provider openai).
# Default coaching is FREE — no .env needed (built-in template coach).
# Optional free local AI: install Ollama + ollama pull llama3.2, then --provider ollama
OPENAI_API_KEY=sk-your-key-here
